# tomgreen.ai — Load-Bearing Type P0 report

Date: 27 August 2026
Branch: `codex/load-bearing-type`
Base: `main` at `ee9a958`
Deployment: not performed

## Decision

Archivo’s width axis is the motion identity. Constraint compresses to `wdth 62`, systems
resolve to `100–106`, and release reaches `125`. Display weight remains fixed. The complete
document is static and resolved without JavaScript or with reduced motion.

## Implemented

- Replaced Newsreader with self-hosted Archivo variable output including `wdth 62–125`.
- Rebuilt the palette, grid, spacing and motion tokens around white, near-black, live green
  and reconstruction clay. Removed category and heat tokens.
- Retyped the persistent header and preserved route coordinates, progress and pending marks.
- Replaced the Home operating field and 280vh operating sequence with the 240svh type-resolve
  journey, a verified four-figure band, white Work bridge, the sole dark Home band and contact.
- Rebuilt Work as six full-row links with tiered type/rule hierarchy and matching hover/focus.
- Added compressed outgoing and resolving incoming route states. Work-to-case navigation adds
  a travelling company-name handoff.
- Rebuilt case-study mastheads around the company name and added Zalando Evidence Object 1:
  120 generic role titles resolving into a leadership spine, six-month ruler and reconstructed
  country split, followed by the verified evidence footer.
- Added the labelled Systems maturity width index while preserving the semantic index, keyboard
  controls, WebGL fallback and context-loss behavior.
- Removed the About corridor. The complete linear route remains available locally and stays 404
  on every Vercel deployment, absent from navigation and sitemap.
- Added production tests for the type journey, no-JS/reduced-motion states, six-row Work index,
  travelling-name navigation, 120-role evidence object, maturity labels, responsive layout,
  WebGL fallback and accessibility.

## Validation

- Lint, typecheck and optimized Next.js build pass.
- 31 unit tests pass.
- 24 Playwright tests pass against `next start`, including six Axe scans with zero serious or
  critical findings.
- The Vercel contract test confirms `/about` is 404 and absent from navigation and sitemap.
- Required layouts pass without horizontal overflow at 1440, 1005, 768 and 390px.
- Review captures include every required viewport plus reduced-motion and no-JS states.
- Initial-page preloaded fonts total 142.5KB woff2; custom Home transition/resolve chunks total
  approximately 2.2KB gzip.

## Known gaps

- The commissioned matte object, portrait and legal-cleared artifacts do not exist yet and were
  not simulated.
- Chapter 2 still uses the shared reconstructed workflow; its sentence-fork evidence object is P1.
- Systems retains the current interactive field until the commissioned object is available.
- Production Web Vitals require observation after deployment; no production action was taken.

## Exact P1 tranche

1. Build Chapter 2 Evidence Object 2, the typeset human/agent sentence fork, from the existing
   five workflow steps and verified footer.
2. Integrate the commissioned matte object on Systems with drag orbit, DPR ≤2, idle/offscreen
   sleep and a pre-rendered context-loss/no-WebGL still.
3. Add the legal-cleared, lazy redacted-artifact layer.
4. Retype the local About career line fully in the axis grammar.
5. Add the approved portrait masthead with responsive crops and provenance.
