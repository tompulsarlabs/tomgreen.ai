# tomgreen.ai — recovery build plan: "Evidence under load"

Date: 28 August 2026
Companion to: [`RECOVERY-REVIEW-REPORT.md`](RECOVERY-REVIEW-REPORT.md) (evidence and register)
Base: `codex/load-bearing-type` head; build executes on this branch, one commit per slice.

## The target, in plain language

The version of the site we want already half-exists across three states. It is:

> The current branch's **chassis** — white ground, the Archivo width grammar, the protected
> route transitions, the clean accessibility and fallback state — carrying **main's evidence
> density** — the owner's wording, the attributed outcomes, the live Ivy/GitHub proof layer,
> the reconstruction disclosures — with **one dimensional signature** that models the method
> instead of decorating it: the Operating Orbit on Systems.

A visitor's journey through the target site: the thesis resolves under their scroll (recognition);
attributed outcomes and two clearly-ranked flagships explain what happened (comprehension); the
live build record and the orbit prove the method is real and running (conviction); contact is one
obvious step. Nothing dark, nothing gated, nothing invented, and every claim inspectable —
because the mechanism that makes it inspectable is back on the page.

## Execution waves

Status keys: **UNGATED** — pre-authorised by the recovery brief's non-negotiables, build
immediately. **GATED** — starts on one named owner decision.

### Wave 0 — Truth and recovery (UNGATED; report §10 R1–R4)

| Slice | Scope (files) | Content |
|---|---|---|
| R1 Truth first | `src/app/page.tsx`, restore `src/components/proof-strip.tsx` + `contribution-graph.tsx` (from `ee9a958`, retyped to current system), `src/lib/content/case-studies.ts` (4 disclosure sentences + 2 headings), case template (outcome de-dupe) | Live-evidence layer back; disclosures back; outcomes attributed; duplicates removed |
| R2 Restore routes | `src/app/page.tsx`, `src/app/work/page.tsx`, ledger last-known-good quotes | Home's six deleted blocks recomposed white; /work deck, bands, metric rail, six summaries |
| R3 Ship surface | `src/app/opengraph-image.tsx`, `src/app/globals.css` (release clamp), `e2e/verify-vercel-about.mjs` (rebuild with VERCEL=1), new `e2e/content-guard` | OG on-system; short-desktop overflow fixed; contract script truthful; content-regression CI guard |
| R4 Transition refinements | `src/components/route-transition.tsx`, `src/app/globals.css` | Atomic swap, click-guard scope, Back cancellation, clone z-index, uniform scale, two velocity fixes — all inside the protected language |

Acceptance per slice is specified in report §10. Full suite green before every push.

### Wave 1 — The signature (GATED on decision 2; report §10 R6)

Productionise `prototype-operating-orbit.html`: typed `src/lib/orbit-motion.ts` + unit tests,
`src/components/operating-orbit.tsx` (client, gated, poster fallback), Systems masthead
integration, 390 recomposition, build-time still generation wired into CI. Contract in report
§9. Estimated 1–1½ days agentic.

### Wave 2 — Differentiation and polish (UNGATED after Waves 0–1)

Authored-390 decision per route; Systems live-state lines on running records (timestamp +
source link — replaces the imperceptible width channel as the evidence carrier); ambient quiet
width grammar behind a prototype gate; reveal rationing. Then the bounded P2 motion pass
(§4-1 P2 items; Direction C prototype gate).

### Wave 3 — Ship

Human Lusion pass to validate the benchmark principles; Firefox/Safari checks; deployment on
separate approval; production RUM; `/about` live-404 spot check.

## Owner gates (the complete list — everything else is pre-authorised)

1. **Spine** (report §12-1): "Identify constraints. / Simplicity by design. / Build a system
   that compounds." (your later commit — recommended) vs "I see the constraint. / Design the
   system. / Build what makes it move." → applies in R5, one hour.
2. **3D direction** (§12-2): approve A (prototype attached) → starts Wave 1.
3. **/work deck-era copy** (§12-3): restore / rewrite / drop, per ledger row.
4. **Execution in public** (§12-4): restore mechanism (R1 default, recommended) vs delete the
   claims.
5. **Case-study section voice** (§12-5): main's voice (R2 default) vs current's plainer labels.

Answering 1–2 unblocks everything; 3–5 have safe defaults that R2 applies and you can reverse
by commit.

## Overnight execution log (updated as slices land)

- [x] Direction A prototype + stills + sketches — committed (`ac66369`)
- [x] R1 Truth first — `941c1ca` (suites green)
- [x] R2 Restore routes — `140bd43` (suites green)
- [x] R3 Ship surface — `670e045` (content-guard + Vercel contract pass)
- [x] R4 Transition refinements — `cf73b04` (velocity 31.0/18.8 vs 40 ceiling)
- [x] Review report committed; verified ledger table inserted (77 rows, zero refuted)
- [x] Draft PR #2 opened against `codex/load-bearing-type`
- [ ] R5 (gated: spine decision) · Wave 1 (gated: 3D direction decision)

Every slice lands as its own commit on this branch with suites green, so the morning review is
commit-by-commit; nothing merges without approval.
